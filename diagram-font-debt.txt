# 可読性の下限を満たしていない図。tools/check_diagram_fonts.py が読む。
#
# このファイルは減る方向にしか動かせない。未記載の違反は失敗し、記載されているのに
# 下限を満たすようになったファイルも失敗する（行を消す強制力）。行の追加は可能だが、
# 図の変更と同じ diff に現れるのでレビューで見える。
#
# 残っているのは atx-fsxn-migration-overview の 4 ファイル。1500px のキャンバスに
# fontSize 11 / 12 で、880px のカラムに収まると実効 6.5px になる。
#
# 補足ボックスは両図から削除済み（実測値はすべて docs/ja|en/atx-fsxn-ga-verification.md
# にあり、消えたのは二重管理）。finalize-flexclone はそれと縦積みへの変更で下限を満たし、
# ラチェットに従ってこのファイルから 4 行が消えた。
#
# **overview に残っているのは寸法の決定である。** この図は 5 段の直列と 2 本の並列分岐を
# 持ち、最長ラベルが 16px で 224px あるため（「（レプリケーションサーバー）」と
# "Amazon Elastic Block Store"）、横 1 列に並べると 880px のカラムには入らない。
# 取れる形は 2 つで、どちらも記事側に影響する。
#
#   1. 1 図のまま縦に積む: 760 × 1550 になる。縦横比が 1:2 で、記事では長い縦スクロール
#      になる。ファイル名と alt text は変わらない。
#   2. データ経路と管理経路の 2 図に分割する: どちらも横長のまま収まる。図が 1 枚増えるので
#      ファイル名・alt text・キャプションと、記事本文の参照が変わる。
#
# 経緯と規範: ~/.kiro/steering/global-document-readability.md
docs/_assets/diagrams/atx-fsxn-migration-overview.drawio
docs/_assets/diagrams/atx-fsxn-migration-overview-en.drawio
docs/_assets/diagrams/atx-fsxn-migration-overview-dark.drawio
docs/_assets/diagrams/atx-fsxn-migration-overview-en-dark.drawio
